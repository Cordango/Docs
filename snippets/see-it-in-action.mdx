<Columns cols={2}>
<Column>

**What a person wrote**

```
expenses/
  cordango.yaml
  apps/expenses/
    app.cordango.yaml
    entities/
      expense_claim.cordango.yaml
    roles/
      admin.cordango.yaml
      approver.cordango.yaml
      employee.cordango.yaml
    views/
      screens/
        overview.cordango.yaml
        claims.cordango.yaml
        my_expenses.cordango.yaml
      collections/
        all_claims_table.cordango.yaml
        inbox_table.cordango.yaml
        my_claims_table.cordango.yaml
      entities/expense_claim/
        detail.cordango.yaml
    workflows/
      lifecycles/
        claim_flow.cordango.yaml
```

</Column>
<Column>

**What the generator wrote**

```
generated/expenses/
  api/
    Program.cs
    Expenses.Api.csproj
    Entities/
      ExpenseClaim.cs
    Data/
      AppDbContext.cs
      ExpenseClaimConfiguration.cs
    Controllers/
      ExpenseClaimController.cs
    Migrations/
      00000000000001_InitialCreate.cs
    Security/AppRoles.cs
    Workflows/AppWorkflows.cs
    Identity/AppIdentity.cs
  web/src/
    pages/OverviewPage.vue
    pages/ClaimsPage.vue
    pages/MyExpensesPage.vue
    pages/ExpenseClaimRecordPage.vue
    blocks/   40 components
    views/    7 screens of its own
  Dockerfile
  docker-compose.yml
```

</Column>
</Columns>

The right column is an excerpt. The real build writes 103 files from 14.

<Note>
The right side is the `dotnet-vue` [target](/concepts/targets), which generates ASP.NET Core, EF
Core, PostgreSQL, Vue 3 and Vuetify. It is one target. Another would produce something different
from the same left column.
</Note>
